import { PortfolioConstructionLoading } from '@/modules/portfolio-construction';

/** Route-level loading state for the Portfolio Construction Engine. */
export default function PortfolioConstructionLoadingRoute() {
  return <PortfolioConstructionLoading rows={6} />;
}
