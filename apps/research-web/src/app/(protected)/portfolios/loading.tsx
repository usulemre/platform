import { PortfolioLoadingState } from '@/modules/portfolio';

/** Route-level loading state for the portfolios catalog. */
export default function PortfoliosLoading() {
  return <PortfolioLoadingState />;
}
