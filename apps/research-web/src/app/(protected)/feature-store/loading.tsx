import { FeatureStoreLoading } from '@/modules/feature-store';

/** Route-level loading state for the Feature Store. */
export default function FeatureStoreLoadingRoute() {
  return <FeatureStoreLoading rows={6} />;
}
