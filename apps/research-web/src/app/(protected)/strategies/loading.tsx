import { StrategyLoadingState } from '@/modules/strategy';

/** Route-level loading state for the strategies catalog. */
export default function StrategiesLoading() {
  return <StrategyLoadingState />;
}
