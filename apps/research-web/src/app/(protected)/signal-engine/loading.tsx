import { SignalEngineLoading } from '@/modules/signal-engine';

/** Route-level loading state for the Signal Engine. */
export default function SignalEngineLoadingRoute() {
  return <SignalEngineLoading rows={6} />;
}
