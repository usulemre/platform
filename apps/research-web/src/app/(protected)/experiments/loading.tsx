import { ExperimentLoadingState } from '@/modules/experiment';

/** Route-level loading state for the experiments list. */
export default function ExperimentsLoading() {
  return <ExperimentLoadingState />;
}
