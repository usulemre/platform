import { ResearchLoading } from '@/modules/research';

/** Route-level loading state for the Research Engine. */
export default function ResearchLoadingRoute() {
  return <ResearchLoading rows={6} />;
}
