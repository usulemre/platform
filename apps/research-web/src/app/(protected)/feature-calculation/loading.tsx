import { CalcLoading } from '@/modules/feature-calculation';

/** Route-level loading state for the Feature Calculation Engine. */
export default function FeatureCalculationLoadingRoute() {
  return <CalcLoading rows={6} />;
}
