import { MonitorLoading } from '@/modules/monitoring';

/** Route-level loading state for the monitoring dashboard. */
export default function MonitoringLoading() {
  return <MonitorLoading rows={6} />;
}
