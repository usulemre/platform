import { NotificationLoading } from '@/modules/notification';

/** Route-level loading state for the Notification Center. */
export default function NotificationsLoading() {
  return <NotificationLoading rows={8} />;
}
