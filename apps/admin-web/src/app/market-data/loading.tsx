import { MarketLoading } from '@/modules/market-data';

/** Route-level loading state for the Market Data Platform. */
export default function MarketDataLoading() {
  return <MarketLoading rows={6} />;
}
