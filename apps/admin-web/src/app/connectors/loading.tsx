import { ConnectorLoading } from '@/modules/connector';

/** Route-level loading state for the Connector Management Platform. */
export default function ConnectorsLoading() {
  return <ConnectorLoading rows={6} />;
}
