"""validation-service — the deterministic Validation (Quantitative Engine) bounded context.

The one place significance is adjudicated (VS-1). It hosts deterministic, golden-tested gates that
run with every AI actor suspended (AI-2). The first engine is the Multiple-Testing Budget gate.
"""
from __future__ import annotations

__all__: list[str] = []
